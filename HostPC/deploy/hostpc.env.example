# 复制为 /etc/omniroam/hostpc.env 并 chmod 600（可选）
# 使用 MySQL 时：在 omniroam-hostpc.service 里去掉 -sqlite-users 参数，并取消注释 EnvironmentFile=-/etc/omniroam/hostpc.env
#
# MYSQL_DSN=app:密码@tcp(127.0.0.1:3306)/simple_robotic_arm?parseTime=true
# AUTH_SECRET=至少设一个长随机串作会话签名
